# Usado só pelo docker-compose.yml (npm run db:up). Copie para .env e troque os valores.
# Gere uma senha forte, ex.: node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
